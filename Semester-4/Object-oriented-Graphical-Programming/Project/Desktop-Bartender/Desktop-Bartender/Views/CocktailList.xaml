<UserControl x:Class="Desktop_Bartender.Views.CocktailList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop_Bartender.Views"
             xmlns:resx="clr-namespace:Desktop_Bartender.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding CocktailListViewModel,Source={StaticResource Locator}}">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="0.7*"/>
      <ColumnDefinition Width="1.3*"/>
    </Grid.ColumnDefinitions>

    <Grid Grid.Column="0" Margin="10">
      <Grid.RowDefinitions>
        <RowDefinition Height="40"/>
        <RowDefinition/>
      </Grid.RowDefinitions>
      <Border BorderThickness="0" Grid.Row="0" Background="#F95738"/>
            <Label Grid.Row="0" Content="{Binding Source={x:Static resx:Resources.YourDrinks}}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" FontFamily="Comic Sans MS" FontSize="20"/>
      <ListView Grid.Row="1"/>
    </Grid>
    <local:Cocktail Grid.Column="1"/>
  </Grid>
</UserControl>
